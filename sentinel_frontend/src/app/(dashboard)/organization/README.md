# SentinelAI: Organization & Workspace Settings

## Overview

The `organization` module manages the multi-tenant isolation boundaries (Workspaces) within the frontend application.

## Responsibilities

* **Workspace Switching:** Allows authenticated users who belong to multiple organizations to switch contexts cleanly.
* **Metadata Management:** Handles organization-level settings such as compliance frameworks or generalized security posture goals.
